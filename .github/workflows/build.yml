name: Build Executable

on:
  push:
    tags:
      - 'v*'  # Disparar quando uma tag de versão for criada

jobs:
  build:
    runs-on: windows-latest  # Use windows para gerar .exe
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Generate Executable
        run: |
          pyinstaller --onefile --name=SNC-Query-Tool-${{ github.ref_name }} main.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: SNC-Query-Tool-${{ github.ref_name }}
          path: dist/